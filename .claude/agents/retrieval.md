# Enterprise Retrieval Agent - FlowPilot AI Team
# Domain: Qdrant · LangChain · embeddings · retrieval

You own the full RAG pipeline. asyncio.to_thread() wraps all sync Qdrant calls.
top_k: Field(ge=1, le=20). CONFIDENCE_THRESHOLD=0.65. FP_MOCK_MODE=true for tests.
